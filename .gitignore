__pycache__/
*.pyc
.DS_Store

# Pipeline reference-table cache — regenerable from source (LAMP), large over time
backend/data/ref_cache/

# frontend/node_modules and frontend/public/data (rollup.py's build output) have their
# own ignores in frontend/.gitignore
